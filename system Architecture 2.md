### Technical Communication 2

|Frontend|Backend|Error handling|Acceptance criteria|Design|Acceptance Criteria|
|--------|---------|-------------|-------------------|-------|------------------|
|Request username|GET/CHARACTERS- retrieves numeric, alphabetical and special characters|1)If username >8, frontend highlights topmost in-line validation criteria under the username bar 2) If username excludes a criteria, the in-line validation drop down list criteria excluded is highlighted red|1) Username for device owner is a one time action 2)Frontend must display in-line validation drop down list under the username bar with username content criteria typed in red, when criteria it is met, in-line validation changes to green 2)Username must be ≤8 3) Username must have ≥1 special character, ≥1 numeric character ≥1 upper case character ≥1 lower case character 4) Only new users can select new username if they are not returning, while returning screen users select their username each time they want use device|Onboarding page has an attractive blend of ≤3 colours, preferrably, blue green and white|1) Colors should have independent color settings 2) Colors should be mild in temperature 3) Colours should overlap midway ofthe screen|
|Request user_id|GET/DEVICE_ID|1)If user_id is the same, frontend displays 'sorry, this username already exist'|1) Username must be unique 2) Username must be matched to device to generate unique user_id| |  |
|Request victual eye test|GET/FONT SIZES|Frontend returns the 'type words bar' empty if the wrong word is typed|1) Frontend should display 'Welcome + Username + new usernames (if any) and displays underneath the 'Welcome' message, 'Are you ready for your eye test? 2) If users select yes, from frontend display question, users proceed to victual eye test, but if no, users are asked 'would you like to exit?' 3)Frontend displays alphanumeric font characters from font size 16 to font size 6 by users' input random selection 4) Frontend would display options of right and wrong words for users to select or users input the words they can see. 5) If user types in correct words, the word turns green and disappears from the list of ≤6 words for user should type.|The victual eye test background should be warm with the image of a blinking eye.|1) Eye should blink randomly and not very often. 2) The eye should blink for a longer period when users input the wrong word. 3) The colour of the blinking eye should be warm, preferably white and blue.|
|Request for cambridge colour eye test|GET/{COLOURS}{COLOUR NAMES}|1) The colour 'type bar' should return empty if user types the wrong color name.|1) Fsetsrontend should display a list of right and wrong color options for users to select the correct option. 2) Color names should match colour displayed 3) GET/DEMO/{COLOURS}{COLOUR NAMES}- should help users identify colours better 4) The tone of colour should be attached to the colour name if user doesn't know what exact colour to name and should be acceptable. 5)If user selects correct colors 5times in 5 colours, user proceeds to next page, if not users should have a colour score of ≤ 80%. 6)If ≤80%, frontend asks user 'would you like to try more colours?', if user says 'no', the application automatically detects the background screen display for the user accordingly - mostly a neutral choice screen color display range that falls between those with such color scores.|The Cambridge colour eye test background should be colourful with ≤6 colours that are a mixture of bright and warm.| 1) Bright colors should not overflood warm colours 2) Warm colours should not overflood bright colours 3)Both colours should be proportional in background combination and should intersect mid way of the screen.|
|Request Spectrophotometer test|GET/EYE SENSOR|1)If eye sensor does not display blinking eyes, user should refresh server, frontend error display message should show 'sorry, eye sensor irresponsive', reload page.|1)Eye sensor should blink randomly on loading.2) The eye sensor page should not be responsive to any activity on the screen 3) The eye sensor should monitor users eye coordination as it displays illuminance degrees 4)The blinking eye should reflect users' blinking. 5)After observing users' eye coordination during the process, sets user screen display resolution. 6) Frontend should display 'Spectrophotometre monitoring' with options side by side below the heading 'show', 'don't show'.|The spectrophotometer page should be white|1) The white background should automatically switch its ray illuminance.|
|Select Book mode|GET/BOOK COLOUR|1)If book color reflects rays, eye sensor immediately pops up to protect user' vision|1) GET/BOOK COLOUR = 1/time 2) GET/BOOK COLOUR ≤ 6 3) No 2 book colors should be the same.4)Book mode should reflect no rays. 5) Book mode should retrieve GET/user_victual_eye_test_result |Book color design must be warm and friendly|1) Design must be similar to that of a book|
